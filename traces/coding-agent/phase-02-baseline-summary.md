# Phase 02 Baseline Summary

- Task objective: implement a minimal deterministic baseline and evaluation engine for the 20 synthetic LeadBridge AI cases.
- Files inspected: `AGENTS.md`, `README.md`, `docs/PROJECT_BRIEF.md`, `docs/ARCHITECTURE.md`, `docs/CURRENT_STATUS.md`, `docs/DECISIONS.md`, `docs/EVALUATION_PLAN.md`, `docs/IMPROVEMENT_CHANGELOG.md`, `docs/REPRODUCTION_GUIDE.md`, `evaluation/cases/cases.json`.
- Implementation decisions: use a deterministic basic sales-inquiry automation baseline; enforce a structural runnable-case boundary; separate authoring, runnable, and prediction types; generate JSON and Markdown baseline reports; record the universal approval gate and deterministic-baseline decision in documentation; harden the matcher with general intent precedence and contextual suppression.
- Commands run: `Get-Location`, `git status --short`, `npm install`, `npm run typecheck`, `npm test`, `npm run evaluate:baseline`, `npm run check`, `node` report-parsing checks, `git check-ignore node_modules dist`, `git diff --check`, `git status --short`.
- Failures and retries: `npm install` timed out twice before succeeding with `--strict-ssl=false`; the log showed `UNABLE_TO_VERIFY_LEAF_SIGNATURE` against the npm registry. The initial Vitest tests also failed once because the test files were missing explicit `vitest` imports, which were then added.
- Verification results: `npm run typecheck` passed; `npm test` passed; `npm run evaluate:baseline` passed and wrote `evaluation/results/baseline.json` and `evaluation/results/baseline.md`; independent parsing confirmed 20 per-case results, matching JSON and Markdown scores, and no authoring-field leakage into predictions; `git check-ignore` confirmed `node_modules` and `dist` are ignored; `git diff --check` completed with only CRLF normalization warnings.
- Final outcome: Phase 2 is implemented, verified, and frozen. The authoritative deterministic baseline produced a 20.00% primary score on the 20-case synthetic set, passing LB-009, LB-012, LB-013, and LB-016. The 10.00% broad-language run remains documented as a failed experiment, and the next implementation milestone is the data and tool layer.
